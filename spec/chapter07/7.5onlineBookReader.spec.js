const { User, Library } = require("../../Interview_Questions/chapter07/7.5onlineBookReader");

describe('Online Book Reader', () => {
    it('should allow a user to create a profile and read a book of their choosing.', () => {
        const library = new Library();
        const muata = new User('muata', 'password');
    });
});