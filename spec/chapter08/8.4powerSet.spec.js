const powerSet = require("../../Interview_Questions/chapter08/8.4powerSet");

describe('Power Set', () => {
    it('should return all the subsets of the set.', () => {
        // expect(powerSet(['a','b','c'])).toContain()
        let x = powerSet(['a', 'b', 'c']);
    });
});