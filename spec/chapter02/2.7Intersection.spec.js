describe('Intersection', () => {
    it('will return true if there is an intersection on two linked lists.', () => {
        // this one is  a bit hard to do with my LinkedList implementation.
        // I would have to create the node first then add to list which is not how my Linked list operate.
    });
});