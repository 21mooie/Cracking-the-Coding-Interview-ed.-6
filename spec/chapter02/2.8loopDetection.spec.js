const { NextNode } = require('../../utils/Node');

describe('Loop Detection', () => {
    it('should find the beginning of the loop.', () => {
        // hard to implement here because of my implementation of LinkedList etc.
    });
});